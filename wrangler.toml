# Configuration Cloudflare Pages (config-as-code).
# Le site est un build statique : Cloudflare sert le contenu de .output/public.
# Build command + variables d'environnement se règlent dans le tableau de bord
# (Connect to Git) — voir README, section « Hébergement cible — Cloudflare Pages ».
name = "elan-gym-vaureal"
compatibility_date = "2025-01-01"
pages_build_output_dir = ".output/public"
